# [Phase2 通し課題] ヒント集

> **使い方**：手が止まったときに開いてください。  
> ヒントは「構造・型・進め方」を示しますが、数字・判断・理由は自分で埋めます。  
> コード内の `___` は自分で考えて補完してください。

---

## コマ6：問いの初期設計

### タスク1：KPI候補を3つ以上

「継続率を上げる」を測定可能な数値に変換するには、切り口を3種類に分けると候補が出しやすくなります。

| 切り口 | 意味 | データの例 | あなたが追加する候補 |
|--------|------|----------|-------------------|
| 状態（いるか/いないか） | ある時点で継続中かどうか | `continued`（6ヶ月後フラグ） | ___ |
| 行動（どれだけ使うか） | アプリの利用密度 | `login_rate_m3`（3ヶ月目ログイン率） | ___ |
| 深さ（どれだけ関与するか） | 機能の利用深度 | `achievement_count`（バッジ数） | ___ |

> **問い**：「継続率が改善した」と経営層が判断するには、上の表のうち何を確認すれば十分か？3種類すべて必要か？

---

### タスク2：ロジックツリーで5個の問いに分解

「継続率を上げる」を **Who（誰が）・Why（なぜ）・When（いつ）** の3軸で分解します。
葉ノードを `df` の具体的なカラム名と結びつけながら埋めてください。

```
継続率を上げる
├── Who：どのユーザーが離脱しているか？
│   ├── Q1：___（ヒント：plan_type や login_band などで切れないか）
│   └── Q2：___
├── Why：なぜ離脱するか？
│   └── Q3：___（ヒント：achievement_count や trainer_session は関係するか）
└── When：いつ離脱するか？
    ├── Q4：___（ヒント：1ヶ月目の変数が3ヶ月目を予測できるか）
    └── Q5：___
```

> **形式のコツ**：各葉ノードを「〇〇と△△の間に関係があるか？」という形にすると、使うカラムが自然に決まります。

---

### タスク3：So What? の書き方

1行だけ完成例を示します。残り4個を同じ書式で書いてください。

| 問い（簡略） | So What?（答えが出たら何が変わるか） | 答えやすさ | 効きやすさ |
|------------|--------------------------------------|:---------:|:---------:|
| 1ヶ月目ログイン率が6ヶ月後継続を予測できるか？ | **1ヶ月目の早期介入タイミング**が特定できる | ○ | ◎ |
| Q2：___ | ___ | ___ | ___ |
| Q3：___ | ___ | ___ | ___ |
| Q4：___ | ___ | ___ | ___ |
| Q5：___ | ___ | ___ | ___ |

> **チェック**：5個すべてが◎◎になった場合は設計を見直してください。「答えやすいが経営判断には直結しない問い」は現実に存在します。

---

### タスク4：仮選定の根拠

選んだ問いを以下の3点で確認してください。

- 答えが出たとき「**誰に・何を・いつ**」の3要素が揃うか？
- プロダクトオーナーの「あと一押しで変わる層」に**直接**対応しているか？
- Phase3 で学ぶ「二群の比較（検定）」や「分類モデル」と接続できるか？

> 3点すべてに「はい」と言える問いが有力候補です。

---

## コマ7：分析ストーリーの設計

### タスク1：結論文（1文）

以下の穴埋め構造に当てはめて書いてください（数字は仮でOK、後で更新します）。

```
「___層（条件：___）に対して、
 ___（施策名）を___（タイミング）で実施することで、
 継続率を全体平均の___%から___%へ改善できる。」
```

> **注意**：「〜を上げたい」ではなく「〜が〜になる」という形で書くと、根拠との対応が自然につながります。

---

### タスク2：根拠3つの型

3種類の根拠を以下のタイプで揃えると、ストーリーに論理的な流れが生まれます。
骨格の `___` を `df` から計算した数字で埋めてください。

| タイプ | 役割 | 書き方の骨格 |
|--------|------|-------------|
| **現状確認** | 問題の規模感 | 「あと一押し層はユーザー全体の___%を占め、継続率は___%（全体比 ___pt 低い）」 |
| **介入有効性** | 施策が効きそうな証拠 | 「`push_opt_in==1` の継続率は___%、`==0` は___%（差：___pt）」 |
| **タイミング** | いつ介入するかの根拠 | 「`login_rate_m1` が ___ 未満のユーザーは、6ヶ月後離脱率が全体比 ___倍」 |

> **考えどころ**：「介入有効性」の根拠は `push_opt_in` を**自分でONにした**ユーザーとの比較です。これは「施策でONにさせた場合」と同じ効果を示すといえますか？この限界を根拠に一言添えてください。

---

### タスク3：想定反論2つ

反論は「方法論への疑問」と「解釈への疑問」を1つずつ用意するとバランスが取れます。

**反論1（方法論）の例**：「そのセグメントの閾値（`login_rate_m1` の区切り値）は恣意的では？」

→ 応答の方向：なぜその閾値にしたか説明する。別の閾値でも結論が変わらないか確認する（感度分析）。

**反論2（解釈）**：「プッシュ通知を送ると逆にアプリをアンインストールされるのでは？」

→ 応答の方向：___（自分で考えてください。どんなデータや論拠があれば反論を退けられるか）

---

### タスク4：ピラミッド構造

「結論が頂点・根拠が中段・データが底辺」の3層で整理します。以下の枠を埋めてください。

```
【頂点：結論】
  → （タスク1で書いた結論文をそのまま置く）

【中段：根拠】
  → 根拠1：（現状確認）___
  → 根拠2：（介入有効性）___
  → 根拠3：（タイミング）___

【底辺：データ・分析結果】
  → 根拠1を支えるグラフ・集計：___
  → 根拠2を支えるグラフ・集計：___
  → 根拠3を支えるグラフ・集計：___
```

> **チェック**：底辺の「グラフ・集計」は `df` から実際に作れるものかを確認してください。

---

## コマ8〜9：ユーザーセグメンテーション

### ヒント：RFM スコア化

R・F・M の対応カラムはすでに決まっています。`pd.qcut()` で各変数を1〜3にスコア化してください。

| 軸 | 使うカラム | 方向 | スコア化コード（Rの例） |
|----|----------|------|----------------------|
| R（最近の活動） | `login_rate_m3` | 高いほど R が高い | `pd.qcut(df['login_rate_m3'], 3, labels=[1,2,3])` |
| F（利用頻度） | `workout_freq_m3` | 高いほど F が高い | ___ |
| M（関与深度） | `achievement_count` | 多いほど M が高い | ___ |

スコアを合計した `rfm_score = R + F + M` のとき：

| rfm_score の範囲 | このプロジェクトでの位置付け |
|-----------------|---------------------------|
| 高い層 | 高エンゲージ（介入不要） |
| **中程度の層** | **← 「あと一押し」候補** |
| 低い層 | 低エンゲージ（介入コストに見合わない） |

> **問い**：「中程度」の範囲をどう定義しますか？ `rfm_score` の何点以上・何点以下にしますか？その根拠は？

---

### ヒント：コホート分析

今回のデータには月次のログイン履歴がないため、通常の「行=コホート・列=経過月」の形は作れません。以下のように四半期単位の継続率として代替してください。

```python
cohort = df.groupby('register_quarter')['continued'].mean().reset_index()
cohort.columns = ['登録四半期', '継続率']
```

> **問い**：四半期ごとに継続率が違う場合、その差は何を意味しますか？（季節性？アプリ改善？キャンペーン効果？）差が**あった場合**と**なかった場合**でそれぞれ何が言えますか？

---

### ヒント：ピボット集計

複数変数の組み合わせで継続率を見るには `pivot_table` を使います。`bins` の区切り値を自分で決めてください。

```python
df['login_band'] = pd.cut(df['login_rate_m1'],
                          bins=[0, ___, ___, 1.0],
                          labels=['低', '中', '高'])

pivot = df.pivot_table(
    values='continued',
    index='plan_type',
    columns='login_band',
    aggfunc='mean'
).round(2)
```

> **問い**：`bins` の区切り値を「分位数で決める」か「固定値で決める」か、どちらが「あと一押し層」の特定に適していますか？その理由は？

---

### ヒント：「あと一押し層」の閾値の決め方

以下の3グループの継続率を実際に計算して、表を埋めてください。中段の閾値 `___` が答えの核心です。

| グループ | 条件（`login_rate_m1` を使う場合） | 継続率（計算してください） | 介入の費用対効果 |
|---------|----------------------------------|--------------------------|----------------|
| 高エンゲージ層 | `> 0.85` | ___ % | 低い（放っておいても続く） |
| **あと一押し層** | `∈ [___, ___]` | ___ % | ← ここを高くしたい |
| 低エンゲージ層 | `< ___` | ___ % | 低い（介入コストに見合わない） |

> **ポイント**：3グループの継続率の差が大きいほど、セグメント定義が効いています。差が小さければ閾値を調整してみてください。`login_rate_m1` 以外の変数を条件に加えるとどう変わりますか？

---

### ヒント：初期施策案の書き方

「対象・内容・タイミング」の3要素で書くと具体的になります。「内容」と「タイミング」は、コマ10〜11 の分析結果と対応させてください（今は仮でOK）。

```
対象   ：（上で定義した「あと一押し層」の条件をそのまま使う）
内容   ：___（push_opt_in や trainer_session など、データで有効と示せた変数に対応した施策）
タイミング：___（コマ10-11 の時系列分析で見えた「介入すべき時点」と合わせる）
```

---

### ヒント：財務インパクトの試算

以下の3ステップで計算してください。数値が出るたびに「これは大きい？小さい？」を意識してください。

**ステップ1：あと一押し層の離脱損失**

```python
# 自分で定義した「あと一押し層」の条件を使う
target = df[(df['login_rate_m1'] >= ___) & (df['login_rate_m1'] < ___)]

monthly_loss = target[target['continued'] == 0]['plan_price'].sum()
print(f"あと一押し層の離脱による月次損失：{monthly_loss:,}円")
print(f"6ヶ月換算：{monthly_loss * 6:,}円")
```

**ステップ2：予算内の介入可能人数**

```python
budget = 400_000  # 月次40万円

cost_per_user = ___  # 選んだ施策の1人あたりコスト（事業条件の表から選ぶ）
max_users = budget // cost_per_user
print(f"予算内で介入できる最大人数：{max_users}人")
print(f"あと一押し層（{len(target)}人）のうち何%に届けられるか：{max_users / len(target) * 100:.1f}%")
```

**ステップ3：ROI計算**

```python
# 継続率改善幅は今は仮置きでOK（コマ10-11 の検定結果で更新する）
improvement_rate = ___   # 例：0.05（5pt 改善と仮定）

expected_revenue = improvement_rate * len(target) * target['plan_price'].mean() * 6
intervention_cost = min(max_users, len(target)) * cost_per_user
roi = (expected_revenue - intervention_cost) / intervention_cost * 100

print(f"期待追加売上：{expected_revenue:,.0f}円")
print(f"施策コスト：{intervention_cost:,.0f}円")
print(f"ROI：{roi:.1f}%")
```

> **ROI がマイナスになった場合**、以下の3つのどれで改善できるか考えてください。  
> ① セグメントを絞ってコストを下げる　② 単価の安い施策に変える　③ 改善幅の見積もりを上げる  
> それぞれを正当化するには「どのデータ・どの分析結果」が必要ですか？

---

## コマ10〜11：時系列傾向と施策の効果検証

### ヒント：早期シグナルの確認

「1ヶ月目の情報だけで6ヶ月後の離脱を予測できるか」を確認する手順です。

```python
# ステップ1：頻度低下量を派生変数として作る
df['freq_drop'] = df['workout_freq_m1'] - df['workout_freq_m3']

# ステップ2：freq_drop の大きさ別に継続率をプロット
bins = pd.cut(df['freq_drop'], bins=___)   # ビン数を決めてください
df.groupby(bins, observed=True)['continued'].mean().plot(kind='bar')
plt.ylabel('継続率')
plt.title('ワークアウト頻度低下量と6ヶ月後継続率')
```

> **問い**：グラフで `freq_drop` が大きいほど継続率が下がる傾向が見えた場合、「介入は登録から何ヶ月目が適切か」という問いにどう答えますか？

---

### ヒント：A/B テスト設計書

以下の全行を埋めてください。「判定基準」と「必要サンプルサイズ」の数字は、理由とセットで書くことが重要です。

| 設計項目 | 記入欄 |
|---------|--------|
| A 群（介入あり）の定義 | あと一押し層のうち、___ を行うグループ |
| B 群（対照群）の定義 | あと一押し層のうち、介入を行わないグループ |
| ランダム割り当て方法 | ___ |
| 主要評価指標（Primary） | ___ |
| 「効果あり」の判定基準 | 継続率の差が ___ pt 以上、かつ p < ___ |
| 評価時点 | 介入から ___ ヶ月後 |
| 必要サンプルサイズの根拠 | ベースライン継続率 ___ ・検出したい最小差 ___ ・α = ___ |
| 施策コスト（月次） | ___ 円/人 × ___ 人 = 合計 ___ 円（予算40万円以内か確認） |
| 期待追加売上 | 継続率 ___ pt 改善 × あと一押し層 ___ 人 × 平均月額 ___ 円 × 6ヶ月 = ___ 円 |
| ROI | （期待追加売上 − 施策コスト）÷ 施策コスト = ___ % |

> **「検出したい最小差」の考え方**：「これだけ継続率が改善すれば施策コストに見合う」という事業上の最低ラインです。ROIの計算と行き来しながら、採算ラインになる改善幅を逆算してみてください。統計的に計算するものではなく、ビジネス判断で設定します。ベースライン継続率と合わせて `statsmodels` の検出力計算に入れてください。

---

### ヒント：既存データでの検定と選択バイアス

```python
from scipy.stats import chi2_contingency

ct = pd.crosstab(df['push_opt_in'], df['continued'])
chi2, p, dof, expected = chi2_contingency(ct)
print(f"観察された差：{df.groupby('push_opt_in')['continued'].mean().diff().iloc[-1]:.3f}")
print(f"p 値：{p:.4f}")
```

検定結果を報告した後、必ず以下の文を完成させてください。

> 「`push_opt_in` はユーザーが**自ら選択**したフラグである。  
>  したがって「通知ONのユーザーの継続率が高い」という結果は、  
>  ___ バイアス（バイアスの名前を書く）を含む可能性がある。  
>  この観察データから「プッシュ通知が継続率を上げた」とは言えない。  
>  因果関係を示すには ___ が必要である。」

---

## コマ12〜13：分析計画書の完成

### ヒント：派生特徴量の追加

テンプレートに1例あります。さらに2つ考えてみましょう。3つ目は「単一カラムではなく複数カラムを組み合わせた総合スコア」を目指してください。

| 変数名 | 計算式 | 何を表すか |
|--------|--------|----------|
| `freq_drop` | `workout_freq_m1 - workout_freq_m3` | ワークアウト頻度の低下量 |
| `login_drop` | `login_rate_m1 - login_rate_m3` | ___ |
| `___` | ___ | 複数の変数を組み合わせた「エンゲージメント総合スコア」 |

---

### ヒント：リスクと限界の3点セット

以下の3観点を1段落ずつ書いてください。書き出しの例を参考に、`___` の部分を埋めてください。

| 観点 | 書き出し例 |
|------|-----------|
| データの代表性 | 「本データは ___ のみを対象としており、___ については代表性がない。実際の分析では…」 |
| 因果 vs 相関 | 「`push_opt_in` は自己選択フラグであるため、___ バイアスが含まれる。このため…」 |
| 汎化可能性 | 「仮想データで設定した閾値（例：`login_rate_m1 = ___`）は実データでは ___ が必要であり…」 |

---

### ヒント：Phase3〜5 接続表の埋め方

テンプレートにすでに4行入っています。「本プロジェクトでの活用」の列を埋めるときは「**どの問いの、どのステップで使うか**」を1行で書くと具体的になります。

| Phase3 コマ | 学ぶ手法 | 本プロジェクトでの活用（埋め方の例） |
|------------|---------|----------------------------------|
| コマ14 | pandas | ◎ `df` の生成・集計・派生変数作成の全工程 |
| コマ21-22 | 検定 | ○ ___ （何の検定を、どの問いに使うか） |
| コマ23 | 分類 | ◎ ___ （目的変数・特徴量・問いとの対応） |
| コマ25 | 評価 | ◎ ___ （なぜ accuracy でなく F1 か） |

---

### ヒント：ミニ発表（5分）の構成

以下のスクリプト型を使って発表を組み立ててください。

```
① 問い（30秒）
   「私たちが答えようとした問いは『___』です。
    この問いを選んだのは___だからです。」

② 手法選定（2分）
   「___手法と___手法を比較しました。
    ___を採用した理由は___です。
    ___を採用しなかった理由は___です。」

③ ストーリー（2分30秒）
   「結論は『___』です。
    根拠は ① ___ ② ___ ③ ___ の3点です。
    想定反論として『___』がありますが、___で応えられます。」
```

> **最終チェック**：①の「問い」・②の「手法」・③の「結論」は同じユーザー層・同じ変数を指していますか？ズレがある場合は、どこかの答えを修正してください。
